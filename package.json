{
  "name": "smartpayment-app",
  "version": "1.0.0",
  "description": "Smart Payment Application - Backend & Frontend",
  "scripts": {
    "bagas": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "stop": "taskkill /IM node.exe /F",
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "pm2:start": "pm2 start ecosystem.config.js",
    "pm2:stop": "pm2 stop all",
    "pm2:restart": "pm2 restart all",
    "pm2:delete": "pm2 delete all",
    "pm2:logs": "pm2 logs",
    "pm2:status": "pm2 status"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "concurrently": "^8.2.2",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.18"
  }
}
